storageClass:
  create: true
  name: nfs-csi 
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
  parameters:
    server: nfs.pyxo.network
    share: /volume1/k3s-dev
  reclaimPolicy: Retain
  volumeBindingMode: Immediate
  mountOptions:
    - nfsvers=4.1
